My rough implementation of a neural network to recognize handwritten digits.

The synchronous version uses a sigmoid function for neuron activation and output

The multithreaded version uses ReLU + Softmax 

Thank you to tylin, and by extension Professor Yann LeCun for hosting the database that provided the data that I used.
https://github.com/cvdfoundation/mnist
